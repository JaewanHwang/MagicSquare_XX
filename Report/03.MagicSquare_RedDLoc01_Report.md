# 03. MagicSquare_XX — RED 단계 (D-LOC-01) 보고서

> STEP 3 산출물 · Dual-Track TDD 첫 RED 사이클
> 프로젝트: MagicSquare_XX · 작성일: 2026-06-04
> 상위 문서: `Report/02.MagicSquare_HarnessSetup_Report.md`, `docs/PRD.md`, `docs/TODO_D-LOC-01.md`

## 1. 목적
Harness 구축 이후 첫 RED 사이클을 수행한다. Logic Track(entity)의 첫 테스트
**D-LOC-01**(빈칸 좌표를 row-major로 수집)을 *구현 없이* 실패 테스트로 먼저 고정한다.

## 2. 대상 (Phase / Layer / Track)
| 항목 | 값 |
| --- | --- |
| Phase | RED |
| Layer | entity |
| Track | Logic (D-*) |
| Test ID | D-LOC-01 (FR-LOC-01) |
| 대상 함수 | `find_blank_coords(grid)` (미구현) |

## 3. C2C 추적 (PRD 근거)
| PRD 위치 | 인용 | 연결 |
| --- | --- | --- |
| §5.1 Rule (L35) | "빈칸(0) 하나라도 있으면 → `무효(미완성)`" | 빈칸 식별 필요성 |
| §2 Output (L18) | "무효 시 **틀린 줄의 위치 목록**" | 위치(좌표) 반환 책임 |
| §3.2 성공기준 (L22) | "손 검산 없이 즉시 유효/무효" | 미완성 즉시 판정 전제 |

- To-Do(판단 포함 1개): 격자 각 칸이 0인지 **판단**해 빈칸 좌표를 row-major로 수집.

## 4. RED 설계표
| Test ID | 대상 함수 | Given→Then | Invariant | RED Failure |
| --- | --- | --- | --- | --- |
| D-LOC-01 | `find_blank_coords(grid)` | 빈칸 2개 G1 → `[(2,2),(3,3)]` row-major | 좌표 1-index(1..4) · blank ⇔ cell==0 · row-major 정렬 · 순수함수 | `pytest.fail("RED: D-LOC-01 …")` (의도적 실패) |

## 5. 산출물 (tests/ 만)
| 파일 | 내용 |
| --- | --- |
| `tests/conftest.py` | `grid_g1` 픽스처 — 빈칸 0이 (2,2)·(3,3)인 4×4 리터럴 격자(로직 없음) |
| `tests/entity/test_d_loc_01.py` | `TestDLoc01.test_d_loc_01_blank_coords_row_major` — AAA 주석 + `pytest.fail` 한 줄 |

## 6. 검증 (RED 확인)
```
python -m pytest tests/entity/test_d_loc_01.py::TestDLoc01::test_d_loc_01_blank_coords_row_major -v
```
- 결과: **1 failed** — `Failed: RED: D-LOC-01 — 구현 없음, 의도적 실패` (`test_d_loc_01.py:9`)
- 컬렉션 성공 → 의도한 실패 메시지로 정상 RED.

## 7. ECB·Mock 규칙 준수
- Logic Track Domain Mock **미사용**(실제 리터럴 격자로 검증).
- entity **E001~E005 emit 없음**(오류 코드는 boundary 전용).
- `entity → *` import 없음, `src/` 미수정.
- assert 완화·skip·xfail **없음**.

## 8. 설계 대비 차이 (1건)
- `entity/constants.py` 상수(34/16/4) import는 모듈 미존재로 컬렉션이 깨질 수 있어
  **GREEN 단계로 보류**. RED 픽스처는 리터럴 격자 데이터만 사용.

## 9. 다음 단계
1. GREEN — `src/entity`에 `find_blank_coords(grid)` 최소 구현, `entity/constants.py` SSOT 상수 도입.
2. 동일 테스트 재실행해 GREEN 확인 후 전체 회귀(`pytest -q`).
3. 후속 RED 후보: D-01(`sumLine`), D-02(`collectLines` 10줄 완전성).
