# 05. MagicSquare_XX — Golden Master (D-LOC-01) 보고서

> STEP 5 산출물 · Approval(Golden Master) 테스트 도입
> 프로젝트: MagicSquare_XX · 작성일: 2026-06-04
> 상위 문서: `Report/04.MagicSquare_GreenDLoc01_Report.md`, `docs/PRD.md`

## 1. 목적
GREEN으로 통과한 **D-LOC-01** 출력에 Golden Master(Approval) 테스트를 도입해,
출력 포맷이 의도치 않게 바뀌면 즉시 회귀로 잡히도록 기준선(approved)을 고정한다.

## 2. 대상 결정 (요청과 차이)
| 항목 | 값 |
| --- | --- |
| 요청 대상 | `D-SOL-01` / `U-OUT-01` |
| 실제 적용 | **D-LOC-01** (entity, Logic) |
| 사유 | `D-SOL-01`/`U-OUT-01`은 `reference.md`·`tests/`에 미존재. Solver는 PRD §4 **범위 밖**. "pytest PASS 전제"를 만족하는 통과 테스트가 D-LOC-01뿐이라 사용자 승인 후 이 대상으로 적용. |

## 3. 산출물
| 파일 | 내용 |
| --- | --- |
| `tests/_approval.py` (신규) | `assert_matches_golden(actual, relative)` — `UPDATE_GOLDEN=1`이면 기준 생성/갱신, 아니면 비교 후 불일치 시 unified diff 보고. 개행 정규화 포함 |
| `tests/golden/d_loc_01_g1.approved.txt` (신규, 코드 생성) | G1 기대 출력 — 1-index `row,col` 줄 포맷 (`2,2` / `3,3`) |
| `tests/entity/test_d_loc_01.py` (수정) | `test_d_loc_01_blank_coords_golden` 추가 + `_format_coords` 포맷 고정 |
| `pyproject.toml` (수정) | `pythonpath`에 `"tests"` 추가 — 헬퍼(`_approval`) import |

## 4. Golden 포맷 (고정)
- 좌표 1-index, 줄당 `row,col`, 행 우선(row-major).
- 파일 말미 개행 1개로 정규화.
```
2,2
3,3
```

## 5. 검증
```
# 기준 생성 (PowerShell)
$env:UPDATE_GOLDEN=1; python -m pytest tests/entity/test_d_loc_01.py::TestDLoc01::test_d_loc_01_blank_coords_golden -v; Remove-Item Env:\UPDATE_GOLDEN
# 검증 (UPDATE_GOLDEN 없음)
python -m pytest tests/entity/test_d_loc_01.py::TestDLoc01::test_d_loc_01_blank_coords_golden -v
```
- 생성: PASSED (기준 파일 코드 생성)
- 검증: **matched** — diff 없음, PASSED
- 전체 회귀: **2 passed**, 실패 0

## 6. 규칙 준수
- 포맷 고정(1-index `row,col`), 에러 코드 문자열 규칙은 향후 boundary 출력에 동일 적용 예정.
- golden은 **코드로만 생성**(수동 편집으로 통과 우회 금지).
- 기존 `assert` 테스트(`..._row_major`)도 유지 — Golden Master는 추가 안전망.

## 7. 다음 단계
1. 범위 내 신규 대상(`validate()` 출력, 예: `D-VAL-*`)을 RED→GREEN 후 동일 Golden Master 연결.
2. boundary 출력(U-*) 도입 시 `assert_matches_golden`로 출력 포맷·에러 코드 문자열 고정.
