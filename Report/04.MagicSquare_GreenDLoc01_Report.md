# 04. MagicSquare_XX — GREEN 단계 (D-LOC-01) 보고서

> STEP 4 산출물 · Dual-Track TDD 첫 GREEN 사이클
> 프로젝트: MagicSquare_XX · 작성일: 2026-06-04
> 상위 문서: `Report/03.MagicSquare_RedDLoc01_Report.md`, `docs/PRD.md`, `docs/TODO_D-LOC-01.md`

## 1. 목적
RED로 고정한 **D-LOC-01**(빈칸 좌표를 row-major로 수집)을 최소 구현으로 통과시킨다.
entity 계층에 순수 도메인 함수 `find_blank_coords(grid)`를 도입하고, 의도적 실패
(`pytest.fail`)를 실제 단언으로 교체해 RED→GREEN 전이를 완료한다.

## 2. 대상 (Phase / Layer / Track)
| 항목 | 값 |
| --- | --- |
| Phase | GREEN |
| Layer | entity |
| Track | Logic (D-*) |
| Test ID | D-LOC-01 (FR-LOC-01) |
| 대상 함수 | `find_blank_coords(grid)` (구현 완료) |

## 3. 실패 원인 진단 (한 줄)
`entity.locator` 모듈·함수가 존재하지 않아 import 불가 + 테스트가 `pytest.fail`로 고정됨.

## 4. 산출물 (src/ + tests/)
| 파일 | 내용 |
| --- | --- |
| `src/entity/constants.py` (신규) | SSOT 상수 — `GRID_SIZE=4`, `BLANK_CELL=0`, `INDEX_BASE=1`, `MIN_VALUE=1`, `MAX_VALUE=16`, `MAGIC_SUM=34` |
| `src/entity/locator.py` (신규) | `find_blank_coords(grid)` — 빈칸 좌표를 1-index row-major로 수집하는 순수 함수 |
| `tests/entity/test_d_loc_01.py` (수정) | `pytest.fail` → `assert find_blank_coords(grid_g1) == [(2,2),(3,3)]` |
| `pyproject.toml` (수정) | `pythonpath = ["src"]` 추가 — 테스트에서 `entity` 패키지 import 가능 |
| `tests/entity/__init__.py` (삭제) | 빈 파일이 `entity` 패키지명을 점유해 `src/entity`를 가리던 충돌 제거 |

## 5. 핵심 구현 요지
- `find_blank_coords`는 `range(GRID_SIZE)` 이중 순회로 `cell == BLANK_CELL`인 칸을
  `(row+INDEX_BASE, col+INDEX_BASE)`로 수집 → 행 우선·열 오름차순(row-major) 자연 정렬.
- 리터럴(0/4/1) 산재 없이 전부 `entity/constants.py`에서 import (SSOT).
- 부수효과 없는 순수 함수.

## 6. 검증 (GREEN 확인)
```
python -m pytest tests/entity/test_d_loc_01.py::TestDLoc01::test_d_loc_01_blank_coords_row_major -v
python -m pytest -v
```
- 타깃 테스트: **1 passed**
- 전체 회귀: **1 passed**, 실패 0 (회귀 없음)
- REPL 스모크: G1 입력 → `[(2, 2), (3, 3)]` (Then 일치)

## 7. ECB·Mock 규칙 준수
- `entity → boundary/control` import **없음**(단방향 의존 유지).
- entity **E001~E005 raise/return 없음**(오류 코드는 boundary 전용).
- Logic Track Domain Mock **미사용**(실제 리터럴 격자로 검증).
- assert 완화·skip·xfail **없음**.

## 8. RED 보고서 대비 해소
- STEP 3에서 GREEN으로 보류했던 `entity/constants.py` 상수 도입을 본 단계에서 완료.
- import 경로 문제(`entity` 패키지 미해결)는 `pythonpath = ["src"]` + 중복 `__init__.py`
  제거로 해소.

## 9. 다음 단계
1. (선택) REFACTOR — 명명·중복 점검(현재 단순해 생략 가능).
2. 후속 RED 후보: `test_d_loc_01_returns_empty_when_no_blank`,
   `test_d_loc_01_multiple_blanks_same_row` (TODO 테스트 플랜).
3. 이후 D-01(`sumLine`), D-02(`collectLines` 10줄 완전성)로 확장.
